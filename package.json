{
  "name": "hugoplate",
  "description": "hugo tailwindcss boilerplate",
  "version": "3.0.0",
  "license": "MIT",
  "author": "zeon.studio",
  "scripts": {
    "dev": "node scripts/generateThemeCSS.js && hugo server",
    "build": "node scripts/generateThemeCSS.js && hugo --gc --minify --templateMetrics --templateMetricsHints --forceSyncStatic",
    "preview": "node scripts/generateThemeCSS.js && hugo server --disableFastRender --navigateToChanged --templateMetrics --templateMetricsHints --watch --forceSyncStatic -e production --minify",
    "dev:example": "cd exampleSite &&  node ../scripts/generateThemeCSS.js && hugo server",
    "build:example": "cd exampleSite && node ../scripts/generateThemeCSS.js && hugo --gc --minify --templateMetrics --templateMetricsHints --forceSyncStatic",
    "preview:example": "cd exampleSite && node ../scripts/generateThemeCSS.js && hugo server --disableFastRender --navigateToChanged --templateMetrics --templateMetricsHints --watch --forceSyncStatic -e production --minify",
    "update-modules": "node ./scripts/clearModules.js && hugo mod clean --all && hugo mod get -u ./... && hugo mod tidy",
    "remove-darkmode": "node ./scripts/removeDarkmode.js && yarn format",
    "project-setup": "node ./scripts/projectSetup.js",
    "theme-setup": "node ./scripts/themeSetup.js",
    "update-theme": "node ./scripts/themeUpdate.js",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.17",
    "@tailwindcss/forms": "^0.5.10",
    "@tailwindcss/typography": "^0.5.19",
    "prettier": "^3.6.2",
    "prettier-plugin-go-template": "0.0.15",
    "prettier-plugin-tailwindcss": "^0.7.1",
    "tailwindcss": "^4.1.17"
  }
}
